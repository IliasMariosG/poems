<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='/public/stylesheets/style.css' type='text/css'/>
  </head>
  <body>
    <ul>
      <!-- <%= message %> -->
      <%message.forEach(poem => {%>
      <li><%= poem.content %></li>
      <% }) %>
    </ul>
  </body>
</html>
